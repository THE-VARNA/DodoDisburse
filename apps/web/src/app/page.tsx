import LandingClient from './LandingClient';

export const metadata = {
  title: 'Global Payout Hub | Dodo x Solana',
  description: 'Fund in Fiat, Payout in Crypto. The ultimate B2B global payout hub built on Solana and Dodo Payments.',
};

export default function RootPage() {
  return <LandingClient />;
}
